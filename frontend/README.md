# Frontend (React + Vite)

## Run

1. Install dependencies:

```bash
npm install
```

2. Start dev server:

```bash
npm run dev
```

3. Build for production:

```bash
npm run build
```

## Notes

- Frontend currently renders generated demo market data.
- API calls can be added through `/api` routes (proxied to backend in development).
